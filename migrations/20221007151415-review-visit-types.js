'use strict'

var dbm
var type
var seed

/**
 * We receive the dbmigrate dependency from dbmigrate initially.
 * This enables us to not have to rely on NODE_PATH.
 */
exports.setup = function (options, seedLink) {
    dbm = options.dbmigrate
    type = dbm.dataType
    seed = seedLink
}

exports.up = function (db) {
    return db.runSql(`
    CREATE TABLE public.review_visit_types(
        id integer primary key generated by default as identity,
        name varchar(10485760) NOT NULL,
        created_at timestamp NOT NULL DEFAULT NOW(),
        updated_at timestamp NOT NULL DEFAULT NOW(),
        resource_id varchar(10485760) NOT NULL,
        CONSTRAINT review_visit_types_resource_id_uq UNIQUE (resource_id),
        CONSTRAINT review_visit_types_name_uq UNIQUE (name)
    );

    CREATE TABLE public.review_has_visit_types (
        id integer primary key generated by default as identity,
        review_id int NOT NULL,
        review_visit_type_id int NOT NULL,
        created_at timestamp NOT NULL DEFAULT NOW(),
        updated_at timestamp NOT NULL DEFAULT NOW(),
        resource_id varchar(10485760) NOT NULL,
        CONSTRAINT review_has_visit_types_resource_id_uq UNIQUE (resource_id),
        CONSTRAINT review_has_visit_types_review_type_id_review_id_uq UNIQUE (review_id, review_visit_type_id),
        CONSTRAINT review_has_visit_types_review_id_fk FOREIGN KEY (review_id) REFERENCES public.reviews(id),
        CONSTRAINT review_has_visit_types_review_type_id_fk FOREIGN KEY (review_visit_type_id) REFERENCES public.review_visit_types(id)
    );

    `)
}

exports.down = function (db) {
    return null
}

exports._meta = {
    version: 1
}
