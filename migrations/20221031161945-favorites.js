'use strict'

var dbm
var type
var seed

/**
 * We receive the dbmigrate dependency from dbmigrate initially.
 * This enables us to not have to rely on NODE_PATH.
 */
exports.setup = function (options, seedLink) {
    dbm = options.dbmigrate
    type = dbm.dataType
    seed = seedLink
}

exports.up = function (db) {
    return db.runSql(`
    CREATE TABLE public.favorites (
        id integer primary key generated by default as identity,
        listing_id int NOT NULL,
        user_id int NOT NULL,
        created_at timestamp NOT NULL DEFAULT NOW(),
        updated_at timestamp NOT NULL DEFAULT NOW(),
        resource_id varchar(10485760) NOT NULL,
        CONSTRAINT favorites_resource_id_uq UNIQUE (resource_id),
        CONSTRAINT favorites_user_id_listing_id_uq UNIQUE (listing_id, user_id),
        CONSTRAINT favorites_listing_id_fk FOREIGN KEY (listing_id) REFERENCES public.listings(id),
        CONSTRAINT favorites_user_id_fk FOREIGN KEY (user_id) REFERENCES public.users(id)
    );
        
    `)
}

exports.down = function (db) {
    return null
}

exports._meta = {
    version: 1
}
